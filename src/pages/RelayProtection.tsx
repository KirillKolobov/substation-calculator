import { Container, Typography } from '@mui/material';

export default function RelayProtection() {
  return (
    <Container maxWidth="lg">
      <Typography variant="h4" gutterBottom>
        Релейная защита
      </Typography>
      <Typography variant="body1">
        Здесь будет реализован модуль расчета уставок релейной защиты.
      </Typography>
    </Container>
  );
}
